{
    "fp16": {
        "enabled": true,
        "loss_scale": 1.0
    },
    "optimizer": {
        "type": "Adam",
        "params": {
            "lr": "auto",
            "weight_decay": "auto"
        }
    },
    "zero_optimization": {
        "stage": 2,
        "offload_optimizer": {
            "device": "cpu"
        }
    },
    "gradient_clipping": "auto",
    "train_micro_batch_size_per_gpu": "auto"
}
